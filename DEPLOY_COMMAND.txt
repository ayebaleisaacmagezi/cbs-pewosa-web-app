cd /root/cbs-pewosa-web-app && git fetch origin WEB-PEWOSA-loan-officer-workspace && git switch WEB-PEWOSA-loan-officer-workspace && git pull --ff-only origin WEB-PEWOSA-loan-officer-workspace && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/WEB-PEWOSA-loan-officer-workspace)" && DEPLOY_SHA=$(git rev-parse --short HEAD) && docker image tag cbs-pewosa-web-app:custom cbs-pewosa-web-app:backup-before-$DEPLOY_SHA && docker build -t cbs-pewosa-web-app:$DEPLOY_SHA . && docker image tag cbs-pewosa-web-app:$DEPLOY_SHA cbs-pewosa-web-app:custom && cd /root/mifosx && docker compose up -d --no-deps --force-recreate web-app && docker compose ps web-app && curl -fI 'http://127.0.0.1:8080/'
